prg_comment = ""
prg_version = "0.7"
def program(prg, cmd):
    prg.add(0, "Initialize 0 TTL and Synchronize.sub")
    prg.add(0, "Initialize Experiment.sub", enable=False)
    prg.add(500, "Config Field OFF.sub")
    prg.add(1500000, "Set MOT NaK.sub")
    prg.add(2000000, "Dark Spot MOT load.sub")
    prg.add(2100000, "Config MOT.sub")
    prg.add(192600000, "Synchronize.sub", enable=False)
    prg.add(197598329, "Pulse TTL2-12", polarity=1, pulse_t=0.25116)
    prg.add(197600000, "All Shutter Close 2017.sub")
    prg.add(197601500, "Config Field OFF.sub")
    prg.add(197604500, "MOT lights Off close.sub")
    prg.add(197610000, "Mirrors Imaging")
    prg.add(197611735, "Gray Molasses 2017")
    prg.add(197671735, "empty.sub")
    prg.add(197671735, "Loading_GM_Q50_MTC200A")
    prg.add(199796645, "B comp x", 665.0)
    prg.add(200005000, "Bcomp y Sign Minus", enable=False)
    prg.add(202605000, "All AOM On.sub")
    prg.add(217605000, "Evaporation Ramp.sub")
    prg.add(654608000, "Decompress Current 200-100", start_t=0.0000, stop_x=100.000, n_points=150, start_x=200.000, stop_t=600.0000)
    prg.add(654615000, "Decompress Voltage 200-100", start_t=0.0000, stop_x=0.000, n_points=150, start_x=30.000, stop_t=600.0000)
    prg.add(782865000, "empty.sub")
    prg.add(782875000, "Mirrors Imaging Bragg")
    prg.add(787865000, "Delta 1 Current ramp", start_t=0.0000, stop_x=50.000, n_points=150, start_x=100.000, stop_t=600.0000)
    prg.add(787870000, "B comp x ramp", start_t=0, stop_x=750, n_points=150, start_x=665, stop_t=600)
    prg.add(797860135, "Picture SetImaging")
    prg.add(797870156, "Picture MOT beams SetImaging")
    prg.add(797880265, "Picture SetRepumper")
    prg.add(802880000, "TTL2-12 ON", enable=False)
    prg.add(802888500, "TTL Repumper MOT ON", enable=False)
    prg.add(802890000, "Pulse uw", polarity=1, pulse_t=0.07200, functions=dict(pulse_t=lambda x: 1e-3*cmd.get_var('tau'), funct_enable=False))
    prg.add(802890000, "TTL Repumper MOT OFF", functions=dict(time=lambda x: x + 1e-3*cmd.get_var('tau'), funct_enable=False), enable=False)
    prg.add(802920000, "Picture NaK Ready no Rep Trig2.sub", functions=dict(time=lambda x: x + 1e-3*cmd.get_var('tau'), funct_enable=False), enable=False)
    prg.add(802920000, "Picture NaK Ready no Rep.sub")
    prg.add(802920100, "Picture Levit 2017 - setup")
    prg.add(802921100, "Delta 1 Current", 13.000)
    prg.add(803411100, "IGBT 4 Open")
    prg.add(803418300, "TTL Repumper MOT ON")
    prg.add(803419800, "TTL Repumper MOT OFF")
    prg.add(803419900, "Picture NaK Ready.sub", enable=False)
    prg.add(803419900, "Picture NaK Ready MOT beams no Rep Trig2.sub", functions=dict(time=lambda x: cmd.get_var('tof'), funct_enable=False))
    prg.add(803519900, "Config Field OFF.sub")
    prg.add(811442040, "TTL2-12 OFF", enable=False)
    prg.add(811542040, "Pulse uw OFF")
    prg.add(811543300, "Set MOT NaK.sub")
    prg.add(812042040, "Dark Spot MOT load.sub")
    prg.add(812142040, "Config MOT.sub")
    return prg
def commands(cmd):
    import numpy as np
    iters = np.arange(80, 150, 4)
    j = 0
    while(cmd.running):
        tau1 = iters[j]
        cmd.set_var('tau', tau1)
        print('\n-------o-------')
        print('Run #%d/%d, with variables:\ntau = %g\n'%(j+1, len(iters), tau1))
        cmd.run(wait_end=True, add_time=100)
        j += 1
        if j == len(iters):
            cmd.stop()
    return cmd
